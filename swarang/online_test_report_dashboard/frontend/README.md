
# Frontend (React)
Dev flow:
- Install dependencies: `npm install`
- Run dev server: `npm run dev` (this uses parcel)
- Production build: `npm run build` and serve the `dist` or use `npm start` which serves the built files.

Notes: The frontend expects the backend at http://localhost:8000
